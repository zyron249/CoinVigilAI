{
  "name": "coinvigil-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf build && solcjs --bin --abi --optimize src/CoinVigilTokenFactory.sol -o build"
  },
  "devDependencies": {
    "solc": "^0.8.30"
  }
}
